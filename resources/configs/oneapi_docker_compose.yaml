version: '3.8'
services:
  oneapi:
    image: one-api:latest
    ports:
      - "8080:8080"
    environment:
      - OLLAMA_URL=http://ollama:11434
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ./models:/models
